apiVersion: v1
kind: Service
metadata:
  name: sperichatgpt2-backend
  labels:
    app: sperichatgpt2
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: sperichatgpt2
    component: backend
